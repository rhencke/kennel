[project]
name = "fido"
version = "0.1.0"
description = "GitHub webhook listener for Fido"
requires-python = ">=3.14"
dependencies = [
  "agent-client-protocol>=0.9.0",
  "frozendict>=2.4.7",
  "requests",
]

[dependency-groups]
dev = ["pyright[nodejs]", "ruff", "pytest", "pytest-cov", "pytest-xdist", "hypothesis"]
