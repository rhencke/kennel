name: Setup Python + uv
description: Install uv with cache and sync dev dependencies

runs:
  using: composite
  steps:
    - uses: astral-sh/setup-uv@v6
      with:
        enable-cache: true
    - run: uv sync --group dev
      shell: bash
