# Generated by ./fido gen-workflows; do not edit by hand.
# Refresh with: ./fido gen-workflows
# Buildx local cache scopes discovered from bake: fido, fido-test, format, generated-typecheck, lint, make-rocq, rocq-image, rocq-repl, test, typecheck
name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  pre-commit:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: ./.githooks/pre-commit
      - name: Check committed model mirrors
        run: |
          ./fido make-rocq
          git diff --exit-code -- src/fido/rocq/
      - name: Check generated workflow files
        run: |
          ./fido gen-workflows
          git diff --exit-code -- .github/workflows/ci.yml tools/build_graph.sh
